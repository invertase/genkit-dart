name: agentic_patterns
description: Agentic patterns samples for Genkit Dart
publish_to: none

environment:
  sdk: ^3.10.0
resolution: workspace

dependencies:
  args: ^2.6.0
  genkit: ^0.13.1
  genkit_google_genai: ^0.2.6
  schemantic: ^0.1.3

dev_dependencies:
  build_runner: ^2.10.5

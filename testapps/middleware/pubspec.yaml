name: middleware_sample
description: Genkit Middleware test app
publish_to: none
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.13.2
  genkit_google_genai: ^0.2.7
  genkit_middleware: ^0.4.2
  schemantic: ^0.1.3

dev_dependencies:
  build_runner: ^2.10.5
  test: ^1.29.0

name: agentic_patterns
description: Agentic patterns samples for Genkit Dart
publish_to: none

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  args: ^2.6.0
  genkit: ^0.10.0-dev.16
  genkit_google_genai: ^0.0.1-dev.16
  schemantic: ^0.0.1-dev.16

dev_dependencies:
  build_runner: ^2.10.5
